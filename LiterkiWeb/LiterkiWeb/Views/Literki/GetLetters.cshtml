@model IEnumerable<Literki>
@{
    ViewData["Title"] = "Literki";
    int i = 0;
}
<div class="carousel">
    <div class="carousel__scene">
        <ol class="carousel__list">
            @*<div class="carousel-inner">*@
            @foreach (var item in Model)
            {
                @*i++;
                var active = i == 1 ? "active" : "";*@
               @* <div class="carousel-item @active">
                    <img class="d-block w-50" src="@item.PictureUrl" alt="@item.Nazwa">
                </div>*@
                <li class="carousel__cell">
                    <span class="song__title">@item.Nazwa</span>
                    <img class="d-block w-30" src="@item.PictureUrl" alt="First slide">
                </li>
            }
            @*</div>*@
        </ol>
        @*<a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only text-dark">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only text-dark">Next</span>
        </a>*@
    </div>
    <div class="carousel__controls">
       @* <button class="previous-button">&#x1F446;&#x1F3FB;</button>
        <button class="next-button">&#x1F447;&#x1F3FB;</button>*@
        <input id type="text" onkeypress="anotherPic()">
    </div>
</div>

@*<div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
    <div class="carousel-inner">
    <div class="carousel-item active">
    <img class="d-block w-100" src="/images/lduze.png" alt="First slide">
    </div>
    <div class="carousel-item">
    <img class="d-block w-100" src="/images/oduze.png" alt="Second slide">
    </div>
    </div>
    </div>*@
